<% provide(:title, 'All problems') %>
<p id="notice"><%= notice %></p>

<h1>All Problems</h1>

<%= will_paginate %>

<table>
  <thead>
    <tr>
      <th><%= link_to "Name", :sort => "name" %></th>
      <th><%= link_to "GG", :sort => "givengrade" %></th>
      <th><%= link_to "Set", :sort => "setter" %></th>
      <% if current_user.admin? %>
        <th></th>
        <th></th>
        <% end %>
      <th>Following</th>
     </tr>
  </thead>

  <tbody>
    <% @problems.each do |problem| %>
      <tr>
        <td><%= problem.name %></td>
        <td><%= problem.givengrade %></td>
        <td><%= problem.setter %></td>
        
        <% if current_user.admin? %>
        <td><%= link_to 'Edit', edit_problem_path(problem) %></td>
        <td><%= link_to 'Remove', problem, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <% end %>
        <td><%= render 'follow_form', problem: problem if logged_in? %></td>
           
         
      </tr>
    <% end %>
  </tbody>
</table>

<% if current_user.admin? %>
<br>
<%= link_to 'New Problem', new_problem_path %>
<% end %>